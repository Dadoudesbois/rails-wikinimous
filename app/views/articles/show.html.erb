<div class="center">
    <h2> <%= @article.title %>
    <%= link_to '🗑️', article_path(@article), method: :delete, data: { confirm: "Say bye bye to this article 😞 ?" } %>
    <%= link_to '🖋️', edit_article_path(@article) %> </h2>
    <p> <%= @article.content %></p>
</div>
